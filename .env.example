# =========================
# VATFix Plus — .env.example
# This file is safe to commit. DO NOT put real secrets here.
# =========================

# --- Stripe (required in prod)
STRIPE_SECRET_KEY=sk_live_xxx                  # e.g. sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_xxx                # e.g. whsec_...

# --- AWS S3 (required in prod)
AWS_REGION=eu-north-1                          # example region
S3_BUCKET=vatfix-plus                          # example bucket name (no secrets)

# --- Site / marketing
MARKETING_ORIGIN=https://plus.vatfix.eu

# --- Checkout (optional fallbacks)
# If you don’t use lookup_keys on /buy, set a fallback price here.
CHECKOUT_PRICE_ID=                             # e.g. price_123
CHECKOUT_SUCCESS_PATH=/success
CHECKOUT_CANCEL_PATH=/cancel
TRIAL_DAYS=                                    # e.g. 3

# --- Zapier embeds (optional; enables on /plus)
ZAPIER_CLIENT_ID=                              # from Zapier Partner portal
ZAPIER_SIGNUP_EMAIL=                           # optional QAC prefill
ZAPIER_SIGNUP_FIRST=
ZAPIER_SIGNUP_LAST=


# --- Mailer (optional; only if emailKey() should send emails)
# Option A: single SMTP URL
SMTP_URL=                                      # e.g. smtp://user:pass@smtp.example.com:587
# Option B: host/port/auth (used if SMTP_URL is empty)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=keys@example.com                     # visible "From" address
MAIL_FALLBACK=ops@example.com                  # used if we don’t know customer email

# --- Webhook behavior (optional)
TRIAL_MIN_SECONDS=0                            # extra trial after checkout (seconds)
GRACE_DAYS_AFTER_END=7                         # grace after subscription ends (days)
