# Contributing to VATFix Plus

Thanks for helping make every checkout unstoppable! This repo contains public docs, Postman assets, and meta files. The production API and webhook live in private repos.

## Quick start (docs site / static assets)

```bash
# clone
git clone https://github.com/vatfix/VATFix-Plus.git
cd VATFix-Plus

# install (if the site has a small node script or preview)
# npm i

# run a simple preview of README if needed (optional)
# npx serve .
